<%= render partial: "shared/flash" %>
<div class="container">
  <div class="row">
	  <div class="col-md-12">
	  	<div class="panel panel-default remove-bottom-margin">
	    	<div class="panel-body">
				<% @links.each do |link| %>
					<%= link.url %>
				<% end %>
				</div>
			</div>
		</div>
	</div>
</div>